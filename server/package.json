{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "@types/node": "^17.0.25",
    "nodemon": "^2.0.15",
    "tslint": "^6.1.3",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint",
    "run": "tsc --project tsconfig.json",
    "nodemon": "nodemon --watch src --ext ts --exec \"npm run run && node dist/src/index.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1132.0",
    "gts": "^3.1.0"
  }
}
